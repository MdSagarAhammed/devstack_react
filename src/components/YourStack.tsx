import type { Technology } from '../types/technology'
import { TrashIcon } from './Icons'
import { StackItem } from './StackItem'
export function YourStack({stack,onRemove,onClear}:{stack:Technology[];onRemove:(id:number)=>void;onClear:()=>void}){return <aside className="h-fit rounded-2xl border border-slate-100 bg-white p-5 shadow-[0_8px_28px_rgba(15,23,42,.05)] lg:sticky lg:top-24"><div className="flex items-start justify-between gap-4"><div><h2 className="text-base font-extrabold tracking-[-0.02em] text-slate-900">Your Stack</h2><p className="mt-1 text-[11px] font-semibold text-slate-400">{stack.length} Technology{stack.length===1?'':'ies'} Selected</p></div>{stack.length>0&&<button onClick={onClear} className="inline-flex items-center gap-1 text-[10px] font-bold text-slate-400 hover:text-rose-500"><TrashIcon className="h-3.5 w-3.5"/> Remove All</button>}</div><div className="mt-5 space-y-2.5">{stack.length===0?<div className="rounded-xl border border-dashed border-slate-200 px-5 py-10 text-center"><div className="mx-auto grid h-10 w-10 place-items-center rounded-full bg-slate-50 text-slate-300">+</div><p className="mt-3 text-xs font-bold text-slate-400">Your stack is empty</p><p className="mt-1 text-[10px] leading-4 text-slate-400">Add technologies from the list to build your ideal stack.</p></div>:stack.map(tech=><StackItem key={tech.id} tech={tech} onRemove={onRemove}/>)}</div>{stack.length>0&&<div className="mt-5 rounded-xl bg-gradient-to-r from-orange-50 via-pink-50 to-violet-50 p-3.5"><p className="text-[10px] font-bold text-slate-500">STACK SUMMARY</p><p className="mt-1 text-xs font-extrabold text-slate-800">{stack.map(t=>t.name).join(' · ')}</p></div>}</aside>}
